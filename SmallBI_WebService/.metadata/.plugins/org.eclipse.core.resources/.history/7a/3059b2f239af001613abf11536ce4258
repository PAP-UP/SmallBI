package entity;

import java.util.Calendar;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;
import javax.persistence.SequenceGenerator;
import javax.validation.constraints.NotNull;

import org.hibernate.validator.constraints.Length;
import org.hibernate.validator.constraints.NotBlank;


@Entity
public class Cidade {
	
	@Id
	@SequenceGenerator(name="seq_cidade", sequenceName="seq_cidade_id", allocationSize=1)
	@GeneratedValue(generator="seq_cidade", strategy=GenerationType.SEQUENCE)
	private Integer idCidade;
	
	@Length(max=10, message="Hibernate Validator: A dataCadastro não pode conter mais de {max} caracteres!")
	@NotBlank(message="A dataCadastro não pode estar em branco!")
	@NotNull(message="A dataCadastro não pode ser null!")
	@Column(name="dataCadastro", nullable=false, length=10)
	private String dataCadastro;
	
	@NotNull(message="O usuario não pode ser null")
	@Column(name="usuario", nullable=false)
	private Usuario usuario;
	
	@Length(max=30, message="Hibernate Validator: O nomeCidade não pode conter mais de {max} caracteres!")
	@NotBlank(message="O nomeCidade não pode estar em branco!")
	@NotNull(message="O nomeCidade não pode ser null!")
	@Column(name="nomeCidade", nullable=false, length=30)
	private String nomeCidade;
	
	@Length(max=2, message="Hibernate Validator: A siglaCidade não pode conter mais de {max} caracteres!")
	@NotBlank(message="A siglaCidade não pode estar em branco!")
	@NotNull(message="A siglaCidade não pode ser null!")
	@Column(name="siglaCidade", nullable=false, length=2)
	private String siglaCidade;
	private Estado estado;
}
